tasks.withType(JavaCompile).configureEach {
    options.compilerArgs << "-Xlint:-processing"
}

// Отключаем строгую проверку миксинов при сборке на GitHub
System.setProperty("mixin.initialiseTargetVerify", "false")
